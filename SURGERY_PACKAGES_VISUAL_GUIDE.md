# Surgery Packages - Visual Architecture & Flow

## 🏗️ System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        FRONTEND LAYER                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌────────────────────────────┐    ┌─────────────────────────┐ │
│  │  IndividualBillingView     │    │ SurgeryPackagesManager │ │
│  ├────────────────────────────┤    ├─────────────────────────┤ │
│  │ • Load Package Dropdown    │    │ • Create Package       │ │
│  │ • Save as Package Popup    │    │ • Edit Package         │ │
│  │ • handleLoadPackage()      │    │ • Delete Package       │ │
│  │ • handleSaveAsPackage()    │    │ • View All Packages    │ │
│  │ • State Management         │    │ (Doctor-only)          │ │
│  └────────────────────────────┘    └─────────────────────────┘ │
│           │                                  │                   │
│           └──────────────┬──────────────────┘                   │
│                          │                                       │
│                   ┌──────▼──────┐                                │
│                   │   API Config │                                │
│                   │   api.ts     │                                │
│                   └──────┬──────┘                                │
│                          │                                       │
└──────────────────────────┼───────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                     API ENDPOINTS LAYER                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─ POST /api/surgery-packages                                 │
│  ├─ GET /api/surgery-packages                                  │
│  ├─ GET /api/surgery-packages/{id}                             │
│  ├─ PUT /api/surgery-packages/{id}                             │
│  └─ DELETE /api/surgery-packages/{id}                          │
│                                                                 │
└──────────────────────────┬───────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                      BACKEND LAYER                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────┐                  │
│  │      main.py - FastAPI Application      │                  │
│  ├─────────────────────────────────────────┤                  │
│  │ • Route handlers                        │                  │
│  │ • Input validation                      │                  │
│  │ • Error handling                        │                  │
│  │ • CORS & Middleware                     │                  │
│  └────────────────────┬────────────────────┘                  │
│                       │                                        │
│  ┌────────────────────▼────────────────────┐                  │
│  │   models.py - Pydantic Models           │                  │
│  ├─────────────────────────────────────────┤                  │
│  │ • SurgeryPackageItem                    │                  │
│  │ • SurgeryPackage                        │                  │
│  │ • SurgeryPackageInDB                    │                  │
│  │ • NewSurgeryPackage                     │                  │
│  │ • UpdateSurgeryPackage                  │                  │
│  └────────────────────┬────────────────────┘                  │
│                       │                                        │
│  ┌────────────────────▼────────────────────┐                  │
│  │   database.py - MongoDB Connection      │                  │
│  ├─────────────────────────────────────────┤                  │
│  │ • surgery_packages_collection           │                  │
│  │ • Database initialization               │                  │
│  └────────────────────┬────────────────────┘                  │
│                       │                                        │
└───────────────────────┼──────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│                   DATABASE LAYER (MongoDB)                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  surgery_packages Collection:                                  │
│  ┌─────────────────────────────────────────┐                  │
│  │ {                                       │                  │
│  │   _id: ObjectId,                        │                  │
│  │   hospitalId: String,                   │                  │
│  │   name: String,                         │                  │
│  │   items: [{                             │                  │
│  │     description: String,                │                  │
│  │     amount: Number                      │                  │
│  │   }],                                   │                  │
│  │   totalAmount: Number,                  │                  │
│  │   createdAt: DateTime,                  │                  │
│  │   updatedAt: DateTime                   │                  │
│  │ }                                       │                  │
│  └─────────────────────────────────────────┘                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagrams

### Flow 1: Creating & Saving Package

```
┌──────────────┐
│ Bill Created │ (with Surgery Items)
└──────┬───────┘
       │
       ▼
┌──────────────────────┐
│ Click "Save & Final" │
└──────┬───────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│ Backend: POST /billing-invoices         │
│ ✓ Saves bill to database                │
│ ✓ Returns success (200 OK)              │
└──────┬──────────────────────────────────┘
       │
       ▼
┌────────────────────────────────────────┐
│ Frontend: Check if surgery items exist │
│ ✓ Filter items by category === Surgery │
└──────┬─────────────────────────────────┘
       │ YES ──→ Show Popup
       │
       ▼
┌─────────────────────────────────────────┐
│ Popup: "Save as Reusable Package?"      │
│ • Input: Package Name                   │
│ • Buttons: Save | Skip                  │
└──────┬──────────────────────────────────┘
       │
    SAVE
       │
       ▼
┌─────────────────────────────────────────┐
│ Extract Surgery Items                   │
│ • Get all items from bill               │
│ • Filter breakdown items                │
│ • Map to {description, amount}          │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│ POST /api/surgery-packages              │
│ Payload: {name, items}                  │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│ Backend Processing                      │
│ ✓ Validate input                        │
│ ✓ Calculate totalAmount                 │
│ ✓ Set hospitalId                        │
│ ✓ Add timestamps                        │
│ ✓ Insert into MongoDB                   │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│ Response: Package with _id              │
│ ✓ HTTP 200                              │
│ ✓ Package data returned                 │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│ Frontend Success Handling               │
│ ✓ Show alert: "Package saved!"          │
│ ✓ Refresh packages list                 │
│ ✓ Return to main view                   │
└──────────────────────────────────────────┘
```

### Flow 2: Loading Saved Package

```
┌──────────────────────────────┐
│ Open Billing View            │
│ • Select Patient             │
│ • View "Add Services & Items"│
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ On Component Mount           │
│ • Fetch /api/surgery-packages│
│ • GET all packages           │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ Render Dropdown if Packages  │
│ • Show "Select Package"      │
│ • List: Name + Amount        │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ User Selects Package         │
│ • onChange triggers          │
│ • Package ID captured        │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ handleLoadPackage() Called   │
│ • Get selected package data  │
│ • Extract all items          │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ Transform Package Items      │
│ • Map to BillingItem format  │
│ • Create surgery breakdown   │
│ • Set all totals             │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ Add Items to Bill State      │
│ • setItems([...items, ...]) │
│ • Items appear in table      │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ UI Updates Automatically     │
│ • Items Table refreshes      │
│ • Totals recalculate         │
│ • Ready to modify/save       │
└──────────────────────────────┘
```

### Flow 3: Managing Packages (Doctor)

```
┌────────────────────────────────────┐
│ Doctor Opens Surgery Packages Menu  │
└──────┬─────────────────────────────┘
       │
       ▼
┌────────────────────────────────────┐
│ Check User Role                    │
│ IF role !== 'DOCTOR'               │
│   ▼ HIDE Component                 │
│ ELSE                               │
│   ▼ SHOW Component                 │
└──────┬─────────────────────────────┘
       │
       ▼
┌────────────────────────────────────┐
│ Load All Packages                  │
│ GET /api/surgery-packages          │
└──────┬─────────────────────────────┘
       │
       ▼
┌────────────────────────────────────┐
│ Render Package Cards               │
│ • Package name                     │
│ • Item count                       │
│ • Total amount                     │
│ • Item breakdown table             │
└──────┬─────────────────────────────┘
       │
    ┌──┴──┬──────────┬──────────┐
    │     │          │          │
CREATE EDIT DELETE SELECT
    │     │          │          │
    ▼     ▼          ▼          ▼
  [NEW] [FORM] [CONFIRM] [USE]
```

---

## 📊 Component State Diagram

### IndividualBillingView State

```
┌─────────────────────────────────────────────────────────┐
│          IndividualBillingView Component               │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │ Package Management State:                        │  │
│  ├──────────────────────────────────────────────────┤  │
│  │ showSaveAsPackagePopup: boolean                  │  │
│  │ ├─ true: Popup visible                          │  │
│  │ └─ false: Popup hidden                          │  │
│  │                                                  │  │
│  │ packageName: string                              │  │
│  │ ├─ Empty: Initial state                         │  │
│  │ ├─ "Standard Cataract..." : User input          │  │
│  │ └─ Cleared: After save                          │  │
│  │                                                  │  │
│  │ isSavingAsPackage: boolean                       │  │
│  │ ├─ false: Ready to save                         │  │
│  │ ├─ true: Saving in progress                     │  │
│  │ └─ false: Save complete                         │  │
│  │                                                  │  │
│  │ savedPackages: Array<Package>                    │  │
│  │ ├─ Empty: No packages yet                       │  │
│  │ ├─ Populated: From /api/surgery-packages        │  │
│  │ └─ Updated: After new package created           │  │
│  │                                                  │  │
│  │ loadingPackages: boolean                         │  │
│  │ ├─ true: Fetching packages                      │  │
│  │ └─ false: Fetch complete                        │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🎨 UI Component Hierarchy

```
App
└── IndividualBillingView
    ├── Header
    │   ├── Back Button
    │   ├── Patient Search
    │   ├── Title
    │   ├── Print Button
    │   └── Save & Finalize Button
    │
    ├── Main Content (grid layout)
    │   ├── Left Column (2/3 width)
    │   │   ├── Patient Info Card
    │   │   │   ├── Avatar
    │   │   │   ├── Patient Details
    │   │   │   └── Status Badge
    │   │   │
    │   │   ├── Add Services & Items Card
    │   │   │   ├── Title
    │   │   │   ├── Saved Packages Dropdown
    │   │   │   │   ├── Select Label
    │   │   │   │   └── Options (if packages exist)
    │   │   │   ├── Search Input
    │   │   │   ├── Service Buttons Grid
    │   │   │   │   └── [Service Cards]
    │   │   │   │
    │   │   │   └── Items Table
    │   │   │       ├── Table Header
    │   │   │       ├── Item Rows
    │   │   │       │   ├── Expand Button (if surgery)
    │   │   │       │   ├── Item Details
    │   │   │       │   ├── Quantity
    │   │   │       │   ├── Price
    │   │   │       │   ├── Total
    │   │   │       │   └── Delete Button
    │   │   │       │
    │   │   │       └── Surgery Breakdown (expanded)
    │   │   │           └── Particulars List
    │   │   │
    │   │   └── Insurance & Payment Card
    │   │       ├── Government Insurance Toggle
    │   │       ├── Insurance Details Form
    │   │       ├── Coupon Code Input
    │   │       └── Payment Method Selection
    │   │
    │   └── Right Column (1/3 width)
    │       └── Bill Summary Card
    │           ├── Subtotal
    │           ├── Tax
    │           ├── Discount
    │           ├── Insurance Section
    │           ├── Grand Total
    │           ├── Print Invoice Button
    │           └── Save & Finalize Button
    │
    └── Save as Package Popup (overlay)
        ├── Modal Background
        └── Modal Content
            ├── Success Icon
            ├── Title
            ├── Description
            ├── Package Name Input
            ├── Save Button
            └── Skip Button

SurgeryPackagesManager
├── Header
│   ├── Title
│   └── New Package Button
│
├── Error Alert (if error)
│
├── Create/Edit Form (if showForm)
│   ├── Form Title
│   ├── Package Name Input
│   ├── Items Section
│   │   ├── Item Rows
│   │   │   ├── Description Input
│   │   │   ├── Amount Input
│   │   │   └── Remove Button
│   │   └── Add Item Button
│   ├── Total Display
│   └── Action Buttons (Save, Cancel)
│
└── Packages List (if not showForm)
    ├── Loading Spinner (if loading)
    ├── Empty Message (if no packages)
    └── Package Cards
        ├── Package Name
        ├── Item Count
        ├── Total Amount
        ├── Item Breakdown Table
        └── Action Buttons (Edit, Delete, Select)
```

---

## 🔐 Data Validation Flow

```
User Input
    │
    ▼
┌─────────────────────────────────┐
│ Frontend Validation             │
├─────────────────────────────────┤
│ • Package name not empty        │ ✓ PASS
│ • Item descriptions not empty   │ ✓ PASS
│ • Amount is numeric > 0         │ ✓ PASS
│ • At least 1 item               │ ✓ PASS
└──────┬──────────────────────────┘
       │
       ▼ (if PASS)
┌─────────────────────────────────┐
│ API Payload Created             │
│ {                               │
│   name: string,                 │
│   items: [{                      │
│     description: string,        │
│     amount: number              │
│   }]                            │
│ }                               │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Backend Validation (Pydantic)   │
├─────────────────────────────────┤
│ • Validate NewSurgeryPackage    │ ✓ PASS
│ • Type check all fields         │ ✓ PASS
│ • Enforce required fields       │ ✓ PASS
│ • Range validation              │ ✓ PASS
└──────┬──────────────────────────┘
       │
       ▼ (if PASS)
┌─────────────────────────────────┐
│ Business Logic                  │
├─────────────────────────────────┤
│ • Calculate totalAmount         │ ✓ OK
│ • Set hospitalId                │ ✓ OK
│ • Generate timestamps           │ ✓ OK
│ • Prepare for storage           │ ✓ OK
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ MongoDB Insert                  │
│ • Create ObjectId               │ ✓ OK
│ • Insert document               │ ✓ OK
│ • Return inserted record        │ ✓ OK
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Response to Frontend            │
│ • Status 200 OK                 │
│ • Full package object           │
│ • MongoDB _id included          │
└─────────────────────────────────┘
```

---

## ⚡ Performance Flow

```
Action: Load Packages
    │
    ▼ (0ms)
┌──────────────────────────┐
│ setLoadingPackages(true) │
└──────┬───────────────────┘
       │
       ▼ (~0ms)
┌──────────────────────────┐
│ Fetch API Initiated      │
└──────┬───────────────────┘
       │
       ▼ (~100-200ms)
┌──────────────────────────┐
│ Network Request          │
│ GET /api/surgery-packages│
└──────┬───────────────────┘
       │
       ▼ (~50-100ms)
┌──────────────────────────┐
│ Backend Processing       │
│ Query MongoDB            │
└──────┬───────────────────┘
       │
       ▼ (~0-50ms)
┌──────────────────────────┐
│ Response Sent            │
└──────┬───────────────────┘
       │
       ▼ (~0ms)
┌──────────────────────────┐
│ Frontend Updates State   │
│ setSavedPackages(data)   │
└──────┬───────────────────┘
       │
       ▼ (~0ms)
┌──────────────────────────┐
│ UI Re-renders            │
│ Dropdown now shows items │
└──────┬───────────────────┘
       │
       ▼ (~0ms)
┌──────────────────────────┐
│ setLoadingPackages(false)│
└──────────────────────────┘

TOTAL: 150-350ms
```

---

## 📌 Key Integration Points

```
┌─────────────────────────────────────────────────────────────┐
│                   Integration Touchpoints                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ 1. Component Mount                                          │
│    └─→ useEffect() → fetchSavedPackages()                  │
│                                                             │
│ 2. Dropdown Selection                                       │
│    └─→ handleLoadPackage() → setItems()                    │
│                                                             │
│ 3. Bill Save Success                                        │
│    └─→ Check isSurgery → showPopup()                       │
│                                                             │
│ 4. Package Save Click                                       │
│    └─→ handleSaveAsPackage() → POST API                    │
│                                                             │
│ 5. API Response                                             │
│    └─→ Success → Alert + Refresh Packages                  │
│    └─→ Error → Alert with Error Message                    │
│                                                             │
│ 6. State Updates                                            │
│    └─→ Trigger UI Re-renders                               │
│                                                             │
│ 7. Doctor Dashboard                                         │
│    └─→ SurgeryPackagesManager with CRUD                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

**Diagram Updated:** January 13, 2025  
**Version:** 1.0  
**Status:** Complete
